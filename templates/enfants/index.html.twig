{% extends 'base.html.twig' %}

{% block title %}Enfants index{% endblock %}

{% block body %}
    <h1>Enfants index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Naissance</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for enfant in enfants %}
            <tr>
                <td>{{ enfant.id }}</td>
                <td>{{ enfant.Nom }}</td>
                <td>{{ enfant.Prenom }}</td>
                <td>{{ enfant.Naissance ? enfant.Naissance|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('enfants_show', {'id': enfant.id}) }}">show</a>
                    <a href="{{ path('enfants_edit', {'id': enfant.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('enfants_new') }}">Create new</a>
{% endblock %}
