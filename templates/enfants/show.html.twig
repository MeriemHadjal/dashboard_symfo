{% extends 'base.html.twig' %}

{% block title %}Enfants{% endblock %}

{% block body %}
    <h1>Enfants</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ enfant.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ enfant.Nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ enfant.Prenom }}</td>
            </tr>
            <tr>
                <th>Naissance</th>
                <td>{{ enfant.Naissance ? enfant.Naissance|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Equipe</th>
                <td>{{ enfant.equipe.Nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('enfants_index') }}">back to list</a>

    <a href="{{ path('enfants_edit', {'id': enfant.id}) }}">edit</a>

    {{ include('enfants/_delete_form.html.twig') }}
{% endblock %}
